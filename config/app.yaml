tailscale:
    state_dir: ./tsnet-state
    hostname: tsctl
    auth_key: ""
    ephemeral: false

logging:
    app_log_path: ./logs/app.log
    level: debug
    format: text

server:
    port: 8080
    host: 0.0.0.0
    mode: debug
