tailscale:
    state_dir: ./tsnet-state
    hostname: tsctl
    auth_key: "CHANGE_TS_AUTH_KEY"
    ephemeral: false

logging:
    app_log_path: ./logs/app.log
    level: debug
    format: text

server:
    port: 8080
    host: 0.0.0.0
    mode: debug