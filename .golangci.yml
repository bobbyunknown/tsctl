version: "2"

run:
  concurrency: 4

linters:
  default: none
  enable:
    - errcheck
    - govet
    - staticcheck
    - ineffassign
    - unused

  exclusions:
    paths:
      - dash/.*
      - vendor/.*
      - tmp/.*

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
